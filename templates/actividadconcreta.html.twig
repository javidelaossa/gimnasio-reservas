{% extends 'base.html.twig' %}

{% block title %}Crear actividad{% endblock %}

{% block body %}
    <div class="container">
        <div class="row mt-3">
            {% for actividad in actividades %}
                <div class="card m-3" style="width: 18rem;">
                    <img src="{{ asset('uploads/imagenes/') }}{{ actividad.nombre.imagen }}" class="card-img-top" alt="imagen-clase">
                    <div class="card-body">
                        <h5 class="card-title">{{ actividad.nombre.nombre }}</h5>
                        <p class="card-text">{{ actividad.nombre.descrip }}</p>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
