{% extends 'base.html.twig' %}

{% block title %}Hello GimnasioController!{% endblock %}

{% block body %}
    <div class="container">
        <div class="row mt-3">
            <div class="col-12 col-lg-6">
                {{ form_start(form) }}
                <h1>{{ actividad.nombre.nombre }}</h1>
                <h4>{{ actividad.nombre.descrip }}</h4>
                <p>Número de sala: {{ actividad.sala.numero }}</p>
                <p>Hora inicio: {{ actividad.hinic|date('H:i:s') }} - Hora fin: {{ actividad.hfin|date('H:i:s') }}</p>
                {{ form_end(form) }}

            </div>
            <div class="col-12 col-lg-6">
                <img class="img-fluid" src="{{ asset('uploads/imagenes/') }}{{ actividad.nombre.imagen }}" alt="Logo">

            </div>
        </div>
    </div>
{% endblock %}
