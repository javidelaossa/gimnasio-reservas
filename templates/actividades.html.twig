{% extends 'base.html.twig' %}

{% block title %}Hello GimnasioController!{% endblock %}

{% block body %}
    <div class="example-wrapper">
        <h1>Hello {{ controller_name }}! ✅</h1>
        {% for actividad in rol %}
            <h1>Hello {{ actividad }}! ✅</h1>

        {% endfor %}
        {{ form_start(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
